{
  "name": "kakao-login",
  "version": "1.0.0",
  "description": "Kakao Login with React and NestJS",
  "scripts": {
    "install:all": "yarn --cwd server install && yarn --cwd client install",
    "dev:server": "cd server && yarn run start:dev",
    "dev:client": "cd client && yarn run dev",
    "dev": "concurrently \"yarn run dev:server\" \"yarn run dev:client\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
